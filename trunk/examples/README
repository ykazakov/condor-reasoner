This directory contains an example for the usage of ConDOR

CONTENTS:

  README                this file
  pizza.owl             pizza ontology in OWL-2 functional syntax
  pizza-tax.owl         the computed taxonomy

Example:

  ../bin/condor < pizza.owl > pizza-tax.owl

will load the ontology from file "pizza.owl", classifies it,
and outputs the sorted taxonomy into the file "pizza-tax.owl"
